/**
 * Shared contracts entrypoint.
 *
 * M0-001 only establishes the package boundary. Concrete contract types should
 * be added in a later contract-first task after the corresponding docs and
 * tests are in place.
 */
export {};
