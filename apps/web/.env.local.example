# Local development: point the frontend at the local API server.
# Copy this file to .env.local to activate.
#
# When unset, the frontend defaults to window.location.origin (same-origin),
# which is correct for production deployments behind a reverse proxy.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
